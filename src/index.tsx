import * as React from "react";
import * as ReactDOM from "react-dom";

import { ColorsBoard } from "./components/ColorsBoard";

ReactDOM.render(
    <ColorsBoard />,
    document.getElementById('root')
);
